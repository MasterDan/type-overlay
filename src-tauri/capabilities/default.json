{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main and overlay windows",
  "windows": ["main", "overlay-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default"
  ]
}
